server:
  port: 8081

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

request:
  timeout-millis: 30000

management:
  endpoints:
    web:
      exposure:
        include: loggers, refresh
  endpoint:
    loggers:
      enabled: true